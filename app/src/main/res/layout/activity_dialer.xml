<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <TextView
        android:id="@+id/tv_dialed_number"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="36sp"
        android:layout_marginTop="32dp"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_contact_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="18sp"
        app:layout_constraintTop_toBottomOf="@id/tv_dialed_number" />

    <GridLayout
        android:id="@+id/dial_pad"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:columnCount="3"
        android:rowCount="4"
        android:layout_marginTop="32dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_contact_name">

        <Button style="@style/DialerButton" android:text="1" />
        <Button style="@style/DialerButton" android:text="2" />
        <Button style="@style/DialerButton" android:text="3" />
        <Button style="@style/DialerButton" android:text="4" />
        <Button style="@style/DialerButton" android:text="5" />
        <Button style="@style/DialerButton" android:text="6" />
        <Button style="@style/DialerButton" android:text="7" />
        <Button style="@style/DialerButton" android:text="8" />
        <Button style="@style/DialerButton" android:text="9" />
        <Button style="@style/DialerButton" android:text="*" />
        <Button style="@style/DialerButton" android:text="0" />
        <Button style="@style/DialerButton" android:text="#" />
    </GridLayout>

    <ImageButton
        android:id="@+id/btn_delete"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@drawable/ic_backspace"
        android:background="?attr/selectableItemBackgroundBorderless"
        app:layout_constraintBottom_toBottomOf="@id/btn_call"
        app:layout_constraintStart_toEndOf="@id/btn_call"
        app:layout_constraintTop_toTopOf="@id/btn_call"
        android:layout_marginStart="32dp" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/btn_call"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="32dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:srcCompat="@drawable/ic_call"
        app:backgroundTint="@color/green_call"
        android:tint="@android:color/white" />

</androidx.constraintlayout.widget.ConstraintLayout>
